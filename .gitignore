# macOS
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon?

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Python
__pycache__/
*.py[cod]
*$py.class
.env
.venv
env/
venv/
.pytest_cache/
.mypy_cache/
build/
dist/
*.egg-info/

.local
.local/

paper.pdf

results
results/

# runs/ is generated output only, and ships empty: a clone gets the directory
# (via its README) and nothing else. Everything needed to produce a run lives
# in configs/ + jmbc/, so nothing under runs/ is a source file.
runs/*
!runs/README.md

benchmarks/
.archive/
.sources/

# Colab runners and local notebook scratch: not part of the published repo.
notebooks/
.ipynb_checkpoints/

# Configs the dashboard generates at runtime (configs/exp/dash-*.yaml).
configs/exp/dash-*.yaml

.bal


# Ignore all dotted folders
.*/
