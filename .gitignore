# macOS
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon?

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Python
__pycache__/
*.py[cod]
*$py.class
.env
.venv
env/
venv/
.pytest_cache/
.mypy_cache/
build/
dist/
*.egg-info/

.local
.local/

paper.pdf

results
results/

# runs/ is generated output only: everything needed to produce a run lives in
# configs/ + jmbc/, so a clone gets the directory (via its README) and nothing
# else -- with one exception. runs/paper-ks-fig34/ is the run behind the paper's
# figures 3 and 4, and ships its protocol, scripts, figures and metrics so the
# dashboard has a real run to display out of the box.
runs/*
!runs/README.md
!runs/paper-ks-fig34/
# Re-include the record: the bare `results/` rule further up would swallow it.
!runs/paper-ks-fig34/results/
# ...but not the raw rollouts: 16 MB of snapshot arrays, ~80% of the clone, and
# the dashboard only reads their file size. Regenerate with rerun_fig34.py
# (which writes them) when plot_fig34.py needs to redraw from raw.
runs/paper-ks-fig34/results/**/rollouts.npz

benchmarks/
.archive/
.sources/

# Colab runners and local notebook scratch: not part of the published repo.
notebooks/
.ipynb_checkpoints/

# Configs the dashboard generates at runtime (configs/exp/dash-*.yaml).
configs/exp/dash-*.yaml

# Baseline timings for a sweep's `reference_csv`: third-party numbers digitized
# from a published figure, kept local. The mechanism is in the sweep schema;
# the data is not ours to redistribute.
configs/reference/

.bal


# Ignore all dotted folders
.*/
