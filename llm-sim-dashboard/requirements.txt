# Dashboard interpreter deps (NOT the JAX/jmbc deps — those live in the
# separate simulation interpreter pointed to by JMBC_PYTHON).
streamlit>=1.30
openai>=1.30
python-dotenv>=1.0
requests>=2.31
pandas>=2.0
pyyaml>=6.0
altair>=5.0
